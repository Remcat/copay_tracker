
      <tr>
        <td><%= appointment.client.first_name %></td>
        <td><%= appointment.occured_on %></td>
        <td><%= appointment.start.strftime("%I:%M %P") %></td>
        <td><%= appointment.end.strftime("%I:%M %P") %></td>
        <td><%= appointment.copay_received %></td>
      </tr>